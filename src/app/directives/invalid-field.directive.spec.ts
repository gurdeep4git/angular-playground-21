import { InvalidFieldDirective } from './invalid-field.directive';

describe('InvalidFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidFieldDirective();
    expect(directive).toBeTruthy();
  });
});
